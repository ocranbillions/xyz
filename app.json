{
  "addons": [
    "heroku-postgresql"
  ],
  "buildpacks": [

  ],
  "env": {
    "JWT_SECRET_KEY": {
      "required": true
    }
  },
  "formation": {
  },
  "name": "xyz",
  "scripts": {
    "migrate": "node_modules/.bin/sequelize db:migrate",
    "seed:all": "npx sequelize-cli db:seed:all",
    "postdeploy": "npm run migrate && npm run seed:all"
  },
  "stack": "heroku-18"
}
